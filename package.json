{
  "name": "weather-viz",
  "version": "1.0.0",
  "description": "Weather visualization platform with Vue3 frontend and Express backend",
  "private": true,
  "scripts": {
    "dev": "concurrently -n client,server -c blue,green \"pnpm --filter weather-viz-client dev\" \"pnpm --filter weather-viz-server dev\"",
    "dev:client": "pnpm --filter weather-viz-client dev",
    "dev:server": "pnpm --filter weather-viz-server dev",
    "build": "pnpm --filter weather-viz-client build && pnpm --filter weather-viz-server build",
    "build:client": "pnpm --filter weather-viz-client build",
    "build:server": "pnpm --filter weather-viz-server build"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
